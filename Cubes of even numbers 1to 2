#include<stdio.h>
void main()
{
  int sum=0,n,i;
    printf("enter a number: ");
    scanf("%d",&n);
    for(i=1;i<n;i++)
    {
        if(i%2==0)
        {
    sum=sum+(i*i*i);
        }
    }
    printf ("%d",sum);
}       
